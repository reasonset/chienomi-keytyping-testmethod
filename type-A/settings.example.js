var settings = {
  // Override test text font-size CSS property.
  // default:
  // 'max(min(2.5vw, 5vh), 20px)'
  fontSize: '',
  
  // Override test text font-family CSS property.
  fontFamily: ''
}